## traefik.yml

# Docker configuration backend
providers:
    docker:
        defaultRule: "Host(`{{ trimPrefix `/` .Name }}.easytentacles.fun`)"
  
# API and dashboard configuration
api:
    insecure: true